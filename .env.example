# Base URL of the backend API.
# Local dev: leave empty to use the Vite proxy (/api -> localhost:5050).
# Production (Vercel): set to your backend URL, e.g. https://gt-backend.vercel.app
VITE_API_URL=
